import React from "react";
import { Container } from "react-bootstrap";

const Contribution = () => {
    return (
        <div>
            <Container>contribution(사진첩)</Container>
        </div>
    );
};

export default Contribution;
