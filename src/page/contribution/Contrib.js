import React from "react";
import Menubar from "../../components/Menubar";

const Contrib = () => {
    return (
        <div>
            <Menubar />
        </div>
    );
};

export default Contrib;
